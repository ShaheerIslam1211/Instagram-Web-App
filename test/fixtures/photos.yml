# == Schema Information
#
# Table name: photos
#
#  id         :bigint           not null, primary key
#  image      :string
#  post_id    :bigint
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

one:
  image: MyString
  post: one

two:
  image: MyString
  post: two
